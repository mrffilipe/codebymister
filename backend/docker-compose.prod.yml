services:
  codebymister.api:
    image: ghcr.io/mrffilipe/codebymister-api:1.0.0
    container_name: codebymister-api
    restart: always
    env_file:
      - .env
    networks:
      - my-network

networks:
  my-network:
    external: true